# Negative probe result — recorded deliberately.
#
# roadmap#27: `llms_txt_published` credited a DECLARED pointer wherever no llms/
# artifact existed. This file IS that artifact. It makes repo_has?(slug,'llms')
# true so the unprobed-pointer fallback stops firing, while carrying no .txt so
# llms_txt_present? stays false. The check therefore returns a TRUE zero, backed
# by a fetched URL and a status code, instead of a false credit.
#
# Same posture as the well-known probe artifacts: a recorded absence is a valid
# measurement. Do not delete this to 'clean up' — deleting it restores the bug.
llms:
  probed: '2026-08-17'
  url: https://developer.chrobinson.com/llms.txt
  verdict: soft-404
  published: false
  http_status: 200
  content_type: text/html
  bytes: 1842
  note: >-
    HTTP 200 serving HTML, not a text llms.txt. A 200 is not evidence.
  reprobed: '2026-09-05'
  reprobe_status: 200
  reprobe_verdict: soft-404
  reprobe_note: >-
    Re-probed 2026-09-05 - developer.chrobinson.com/llms.txt still returns the React SPA shell
    (1,842 bytes of text/html, base href https://inavisphere.chrobinson.com/B2B/Portal/v1/).
    The host is a catch-all: every path under it returns the same shell, including a random
    negative-control path. C.H. Robinson still publishes no llms.txt. The apis.yml pointer that
    claimed this URL has been REMOVED; llms/ch-robinson-worldwide-llms.txt in this repo is an
    API Evangelist GENERATED file and says so in its own header - it is not the provider's.
